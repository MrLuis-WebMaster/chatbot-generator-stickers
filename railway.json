{
  "version": 2,
  "buildCommand": "yarn build",
  "env": {},
  "envSecrets": [],
  "layers": [
    {
      "name": "ffmpeg",
      "command": [
        "apt-get update",
        "apt-get install -y ffmpeg"
      ]
    }
  ]
}
